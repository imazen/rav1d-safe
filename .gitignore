target/
Cargo.lock
*.orig
*.rlib
# Trailing slash matches a directory only, so a `test-vectors` SYMLINK into a
# shared checkout (how a scratch worktree gets the corpus) showed up as
# untracked. Match both.
test-vectors
test-vectors/
fuzz/target/
fuzz/artifacts/
fuzz/corpus/
fuzz/coverage/
fuzz-*.log
*.profraw
*.profdata
perf.data
perf.data.old
.claude/worktrees/
!fuzz/Cargo.lock
!apidoc/Cargo.lock

# Tooling noise
.superwork/
copter-report/
.zenbench/
.external-cache/
Cargo.toml.original.txt
*.rs.local
.workongoing

# Generated build/profiling artifacts (tracked by mistake, now untracked)
rust_out
wasmtime-guest-profile.json
target-*/
