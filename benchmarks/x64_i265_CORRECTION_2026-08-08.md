## CORRECTION 2026-08-08 (applies to the two x64 records below)

The pre-merge sweep's build script hit a silent `git checkout` refusal: an
UNTRACKED `examples/bench_ab_decode.rs` (copied in for trees that predate the
in-tree harness) blocked checkout of any tree that TRACKS that file. The
`audit445` and `attrib` arms therefore silently rebuilt the tree that was
already checked out — pre-merge main `a6a7e232`. Verified 2026-08-08:
`ab-main`, `ab-audit445`, `ab-attrib` are byte-identical (text 3314189 /
data 160216 / bss 1144), and `phase3-run.log` contains both
"untracked working tree files would be overwritten" refusals.

VOID (derived from the two fake arms):
- x64_i265_gap_2026-08-08.meta: the `audit445` and `attrib` columns of every
  table; "audit445's reservation batching ... is also noise on x64"; "the
  aarch64 P3 program does NOT transfer to x86-64"; "the tracker is EXONERATED
  as the x64 bottleneck".
- x64_i265_postmerge_2026-08-08.meta §3: "the sharded tracker per se
  exonerated (attrib tip 762a346c ... measured t=1 220.4 on this rig)" —
  that measurement was of a6a7e232, not 762a346c. The #458 attribution to
  1f09769 is likewise wrong: rebuilt genuine, 762a346c measures ~341 ms/f
  at t=1 — the regression arrives with the sharded-tracker chain itself
  (see issue #458 for the corrected bisect and the fix).

STANDS (genuine arms only):
- pre-merge main/pre445 anti-scaling and their identity/curve numbers
  (pre445 checkout succeeded: its tree does not track the harness file);
- all dav1d columns, all ST profile-harness numbers, the #445-cost-is-noise
  conclusion (main vs pre445), and the post-merge §1 scaling table and §2
  (#457) — those used only verified-genuine builds.

Lesson recorded: arm provenance must be verified from the BINARY (embed
`git rev-parse HEAD` in the harness banner or compare section sizes), not
assumed from the script; and every scripted `git checkout` needs `|| fail`.
