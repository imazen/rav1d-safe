# The 16bpc itx dispatch: what the size sweep's ranking actually named

Branch `perf/itx-hbd-wide`, base `main` @ `2fae4fe` (the sweep's brief said
`b0f70ee`; `main` had moved twice — `b0a00c3` then `2fae4fe`/#482 — and this
work is based on the newest).

Follow-on to [`SIZE_SWEEP.md`](SIZE_SWEEP.md) and issue #455. That round
measured; this one builds. Worst news first.

---

## 0. What is NOT done, and one thing the sweep named that I refuted

**Refuted before it was built: "16bpc itx above 16x16".** That is open item 5 of
issue #455 and one of the two candidates this task named. On the vectors this
campaign measures it is worth **nothing**, and I did not port it.

A shape census — an `__ablate`-gated counter at the `itxfm::Fn::call` seam,
recording `(tx_size, handled)` per call — over the size
ladder and the campaign's own gap vectors:

| vector | 16bpc itx calls | of those, >16x16 (scalar) | scalar share of coeff area |
|---|---|---|---|
| `L3840x2160_420_10b` | 272,949 | **20** (0.01%) | **0.15%** |
| `L3840x2160_444_10b` | 369,945 | **3** (0.00%) | **0.01%** |
| `L2048x1152_420_10b` | 131,067 | **0** | **0.00%** |
| `L1024x576_420_10b` | 26,448 | **9** (0.03%) | **1.05%** |
| `L512x288_420_10b` | 2,145 | **14** (0.65%) | **6.48%** |
| `v4k_8tile_10b` | 369,678 | **0** | **0.00%** |

Committed in full as `benchmarks/itx_16bpc_shapes_2026-08-10.tsv`, regenerated by
`examples/itx_shape_census.rs` under `--features __ablate`.

The self-time profile agrees and is the reason I looked: at 4K 10bpc every
`src::itx_1d::*` leaf **combined** is 16 samples of 42,119 — **0.04%**. The
scalar reference is not what 16bpc itx spends its time on. `<itx::itxfm::Fn>::call`
carries 3.36% of *self* time, but that is the hbd dispatch and driver **inlined
into it**, not the fallback.

Caveat, stated plainly: this is one content class (`sips` downscales of one
photo) at one quality point (q70) plus two campaign vectors, inheriting
`SIZE_SWEEP.md`'s §3 limitation. 32x32/64x64 are common on flat and synthetic
content. **The claim here is narrow: the ladder and the gap vectors do not
support the candidate, so it is not where this session spent its time.** A
content sweep could revive it.

**Also not done:**

- **Nothing here touches 8bpc.** All three changes are inside
  `if BD::BPC == BPC::BPC16`. The 0.6–2.4 MP hump, which is `SIZE_SWEEP.md`'s
  headline and is measured at 8bpc, is **untouched**. The 8bpc arms in the
  tables below are a layout control, not a result.
- **The tracker is now the largest addressable family and I did not touch it.**
  After these changes, at 4K 10bpc, `BorrowTracker::add` is 6.66% of self time
  with `drop_glue` at 1.72%; at 1024x576 8bpc the tracker subtree is 10.13%
  inclusive, from `decode_b` 2.69%, `ctx::CaseSetter::set_disjoint` 2.09%,
  `rav1d_recon_b_intra` 1.65%, `decode_sb` 1.13%. Those are block-context
  arrays, not pixels.
- **`decode_coefs` is 54–59% of self time at both depths and is not addressed.**
  It is the entropy decoder and it is the whole ball game; see §6.
- No inter prediction, no 12bpc timing, no x86_64 run (compile only), no
  `--features unchecked`, no loop restoration (still `enable_restoration = 0`
  in every ladder vector — `SIZE_SWEEP.md` §2's blindness is inherited whole).

---

## 1. The ranking that chose the target

`sample` self-time, `L3840x2160_420_10b`, t=1, 55 s, 42,119 leaves, 2026-08-10,
load-tagged (another agent held a `miri` job at ~100% for the whole session —
see §5). Absolutes are soft; the ordering and the paired ratios are not.

| symbol | share | what it is |
|---|---|---|
| `recon::decode_coefs` | 54.84% | entropy — untouched, see §6 |
| `recon::rav1d_recon_b_intra` | 4.16% | |
| **`<itx::itxfm::Fn>::call`** | **3.36%** | hbd dispatch + driver, inlined |
| `BorrowTracker::add::<true>` | 3.13% | |
| **`itx_arm_hbd::apply1d`** | **3.11%** | the 1-D kernels |
| `BorrowTracker::add::<false>` | 3.00% | |
| **`itxfm_add_dispatch::{closure#0}`** | **1.92%** | the per-row pixel add |
| **`ReconDst::slice_mut`** | **1.27%** | the per-row guard |
| **`_platform_memset`** | **1.01%** | 0.76% of it charged to `Fn::call` |

Summed, the 16bpc itx family is **10.90% of the frame**. Three separate
overheads inside it, none of them transform math:

1. **One DisjointMut guard per row** in the pixel-add loop — `h` registrations
   per transform block. 1.92 + 1.27 + 0.91 (tracker charged to that closure's
   subtree) = **4.10%**.
2. **A 1 KiB scratch zeroed per call**, whatever the shape. 181,768 of the
   frame's 272,949 16bpc transform calls are 4x4, which needs 64 B.
3. **The driver's state arrays forced to the stack.** `[V; MAXDIM]` — 16 NEON
   vectors — indexed by a runtime bound and handed to `apply1d` as a slice of
   unknown length, so LLVM zeroed all 16 lanes per group of four and reloaded
   each element across the 1-D call. Again: the dominant shape uses 4 of the 16.

---

## 2. The three changes

| | commit | what |
|---|---|---|
| **A** | `50e878b` | the pixel-add loop uses `ReconDst::for_rows_mut` — one guard over the block's strided hull when tile threading is off, one narrow per-row guard when it is on |
| **B** | `032d917` | the scratch is sized to `w*h` (five arms, 16/32/64/128/256) instead of to `16*16` |
| **C** | `8a24835` | `inv_txfm_hbd_neon` becomes a shape dispatcher over `txfm_core::<W, H>` (nine instantiations); `apply1d` takes `&mut [V; N]` (three) |

**On A and the campaign's own `block_mut` negative.** Issue #455 records
"`block_mut` holding mutable row guards — null; halving the guard count bought
nothing." That is not this. There, guard COUNT went 2h → h and the cost per
access was unchanged. Here the count goes h → 1 *at t=1*, and the comment being
replaced justified the per-row choice with a CfL measurement — but CfL runs one
kernel call over the block, so its registration count was 1 either way. The
measurement did not transfer to a loop that runs `h` times.

Under tile threading `for_rows_mut` still takes one narrow guard per row, so
the soundness argument for concurrent tiles is unchanged, and the `v4k_8tile:8`
cells in §3 are the control that says so.

---

## 3. Measured

All n=9 complete rounds, `measlock --load-ok`, arms interleaved with a rotating
order inside every cell, dav1d 1.5.4 `--framedelay 1` in the same sweep.
**Load-tagged: `foreign_max` is 1 on 475 of the 855 rows, 2 on 270, 3 on 85, and
4-7 on 25.** Zero cells were discarded. Raw rows and the full per-arm bands:
`benchmarks/itx_16bpc_ab_2026-08-10.{tsv.zst,meta}` and
`itx_16bpc_ab_report_2026-08-10.txt`.

### 3.1 The whole change, per size and depth (`ABC/base`, per-round paired)

`DJ` = the base and ABC **ms/frame bands are disjoint** — that is the comparison
the claim makes, not ours-vs-dav1d, which for two different decoders is a tick
that can never fail.

| cell | t | base ms/f | ABC ms/f | **ABC/base** | band | |
|---|---|---|---|---|---|---|
| `L64x36_420_10b` | 1 | 0.0598 | 0.0584 | **0.9724** | [0.9504..0.9901] | |
| `L256x144_420_10b` | 1 | 0.7047 | 0.6887 | **0.9704** | [0.8742..0.9798] | |
| `L512x288_420_10b` | 1 | 3.186 | 3.088 | **0.9649** | [0.8283..1.0346] | |
| `L1024x576_420_10b` | 1 | 17.94 | 16.95 | **0.9338** | [0.9158..0.9634] | **DJ** |
| `L2048x1152_420_10b` | 1 | 68.91 | 63.91 | **0.9203** | [0.8988..0.9515] | **DJ** |
| `L3840x2160_420_10b` | 1 | 225.8 | 211.0 | **0.9414** | [0.9247..0.9534] | **DJ** |
| `L512x288_444_10b` | 1 | 4.457 | 4.304 | **0.9691** | [0.8882..1.0287] | |
| `L1024x576_444_10b` | 1 | 25.87 | 24.27 | **0.9330** | [0.9170..0.9655] | **DJ** |
| `L3840x2160_444_10b` | 1 | 354.4 | 337.4 | **0.9437** | [0.9399..0.9877] | |
| `v4k_8tile_10b` | 1 | 357.0 | 343.2 | **0.9452** | [0.7209..0.9913] | |
| `v4k_8tile_10b` | **8** | 66.39 | 64.11 | **0.9408** | [0.9271..1.0608] | |
| `L64x36_420_8b` | 1 | 0.0470 | 0.0461 | 1.0011 | [0.9398..1.0182] | |
| `L256x144_420_8b` | 1 | 0.6176 | 0.6147 | 0.9989 | [0.9811..1.0413] | |
| `L512x288_420_8b` | 1 | 2.892 | 2.908 | 1.0043 | [0.9024..1.1314] | |
| `L1024x576_420_8b` | 1 | 15.87 | 16.04 | 1.0096 | [0.9335..1.0211] | |
| `L2048x1152_420_8b` | 1 | 60.13 | 61.58 | 1.0126 | [0.9788..1.2736] | |
| `L3840x2160_420_8b` | 1 | 200.2 | 200.6 | 1.0097 | [0.9897..1.2315] | |
| `v4k_8tile` | 1 | 319.3 | 321.9 | 1.0084 | [0.9939..1.0278] | |
| `v4k_8tile` | **8** | 57.78 | 58.56 | 1.0000 | [0.9689..1.5706] | |

**Every 10bpc cell improves; no 8bpc cell moves outside its own noise, and not
one 8bpc band is disjoint.** That is the shape the diff predicts — all three
changes are inside `if BD::BPC == BPC::BPC16`. The 8bpc rows are the control,
and their spread (medians 0.999-1.013, bands out to 1.27) is the code-layout
noise floor for this repo, not a regression.

### 3.2 The depth penalty, which is what Q2 of the size sweep asked about

`SIZE_SWEEP.md` Q2: *"We pay 11-34% to go 8->10 bpc at every size; dav1d pays
1-5%."* Same-size 10bpc/8bpc ratio, 4:2:0, paired inside each round:

| size | base | **ABC** | dav1d |
|---|---|---|---|
| 64x36 | 1.3009 | **1.2598** | 1.0440 |
| 256x144 | 1.1456 | **1.1127** | 1.0247 |
| 512x288 | 1.1018 | **1.0610** | 1.0219 |
| 1024x576 | 1.1323 | **1.0569** | 1.0367 |
| 2048x1152 | 1.1445 | **1.0419** | **1.0486** |
| 3840x2160 | 1.1256 | **1.0484** | 1.0360 |

From **+10.2 to +14.6%** down to **+4.2 to +6.1%** at every size at or above
512x288 — and at **2048x1152 our depth penalty is now smaller than dav1d's**
(1.0419 against 1.0486), the first cell in this campaign where that is true.
The thumbnail keeps a large penalty (1.2598 against dav1d's 1.0440): at 64x36 a
frame is one superblock wide and nothing amortises.

### 3.3 The gap to dav1d

10bpc `ours/dav1d`, same rounds:

| cell | base | **ABC** |
|---|---|---|
| `L64x36_420_10b` | 1.6321 | **1.5870** |
| `L256x144_420_10b` | 1.2694 | **1.2352** |
| `L512x288_420_10b` | 1.2555 | **1.2185** |
| `L1024x576_420_10b` | 1.5714 | **1.4764** |
| `L2048x1152_420_10b` | 1.6321 | **1.5153** |
| `L3840x2160_420_10b` | 1.4075 | **1.3218** |
| `L1024x576_444_10b` | 1.6410 | **1.5359** |
| `v4k_8tile_10b` t=1 | 1.4024 | **1.3256** |
| `v4k_8tile_10b` t=8 | 1.6285 | **1.5233** |

The 10bpc hump peak (2.36 MP) drops 1.632 -> 1.515. **The hump is still there**
— 1.52 at 2.36 MP against 1.22 at 0.15 MP — because its cause is 8bpc-side and
per-block, and nothing here touches that.

### 3.4 Per-change attribution, including one null

Incremental, per-round paired medians over the 10bpc cells:

| step | range over the nine 10bpc cells |
|---|---|
| **A** (`for_rows_mut`) `A/base` | 0.9605 .. 0.9892 |
| **B** (exact-size scratch) `AB/A` | 0.9909 .. **1.0016** |
| **C** (shape-const driver) `ABC/AB` | 0.9721 .. 0.9883 |

**Read incrementally, B is a null — and that reading is WRONG.** `AB/A` is
0.9909..1.0016 with overlapping bands, so the first sweep says the exact-size
scratch buys nothing. A second sweep says otherwise, and the reason is the same
"two costs multiply" shape issue #455 already records for the barrier and the
tracker.

### 3.4b B and C compose: B alone is a null, B on top of C is 0.9-1.8%

A second n=9 sweep (`benchmarks/itx_16bpc_compose_2026-08-10.tsv.zst`) with an
**`AC` arm — A and C, with B reverted** — plus `HEAD` (A+B+C+census) and `base`:

| cell | t | base ms/f | HEAD ms/f | HEAD/base | **HEAD/AC** (= B's effect) | band |
|---|---|---|---|---|---|---|
| `L1024x576_420_10b` | 1 | 17.81 | 16.71 | 0.9338 | **0.9872** | [0.9761..1.0083] |
| `L1024x576_420_10b` | **8** | 16.45 | 15.24 | 0.9294 | **0.9895** | [0.9862..1.0054] |
| `L2048x1152_420_10b` | 1 | 68.93 | 63.47 | 0.9219 | **0.9821** | [0.9746..**0.9952**] |
| `L3840x2160_420_10b` | 1 | 225.2 | 212.1 | 0.9432 | **0.9907** | [0.9780..**0.9923**] |
| `L3840x2160_420_10b` | **8** | 208.1 | 193.6 | 0.9324 | **0.9844** | [0.9822..**0.9931**] |

**B is worth 0.9-1.8%, with the band entirely below 1.0 on three of five cells**
— but only once C has landed. Without C the driver's `[V; MAXDIM]` stack traffic
dominates and a 1 KiB L1-resident `memset` hides inside it; with C the driver is
cheap enough for the `memset` to surface. `_platform_memset` falls 426 -> 169
samples (1.01% -> 0.40% of the frame) across the same pair of profiles, which is
the mechanism. **`032d917` stays.**

The HEAD/base column also reproduces §3.1 from an independent sweep: 0.9432 at
4K 10bpc against 0.9414, 0.9338 at 1024x576 against 0.9338.

### 3.4c The census commit is perf-neutral, measured

`7bc62f3` adds a counter to `itxfm::Fn::call` that is a no-op without
`__ablate`. Same sweep, `HEAD/ABC`: **0.9946 / 0.9985 / 0.9989 / 0.9946 /
0.9978**, every band straddling 1.0 ([0.9803..1.0222] worst). Neutral.

### 3.4d The hull guard is never taken while the shard set is concurrent

This is the one soundness-shaped perf risk in change A, and it is structural
rather than lucky. The brief's null table records that turning the loop filter's
`h` per-row guards into one strided hull was **2.65x SLOWER at t=8**, because a
50-60 KB hull lands on the borrow tracker's wide path and locks all 128 shards.
The itx hull is the same order — a 16x16 block at 4K 16bpc spans
`15*3840 + 16` pixels, ~115 KB.

It cannot land on the wide path, because **both latches are driven by the same
`n_tc > 1` and both are monotone** (`src/lib.rs:177-181`):
`set_tile_threading(n_tc > 1)` and `set_parallelism(n_tc)`, where
`set_parallelism` picks `SHARDS_CONCURRENT` only for `n > 1` and `SHARDS_SERIAL`
(= 1 since #458) otherwise. So `tile_threading_active() == false` — the only
case in which `for_rows_mut` takes a hull at all — implies the tracker has
exactly **one** shard, i.e. `add`'s one-lock fast path, no wide promotion.

Measured, not just argued: the t=8 rows above are single-tile 4K and 1024x576
streams at `--threads 8`, where `tile_threading_active()` is true and the add
loop takes per-row guards. They are **0.9324 and 0.9294**, not 2.65x slower.
`v4k_8tile:8` in §3.1 is the genuinely-8-tile version of the same control, at
1.0000 for 8bpc (untouched) and 0.9408 for 10bpc.

### 3.5 Where the itx family went

`sample` self-time, `L3840x2160_420_10b`, t=1, 55 s each, base 42,119 leaves and
ABC 42,170 (`benchmarks/itx_16bpc_selftime_2026-08-10.txt`):

| symbol | base | ABC |
|---|---|---|
| `<itx::itxfm::Fn>::call` | 3.36% | **1.34%** |
| `itx_arm_hbd::apply1d` | 3.11% | 1.16% (`::<16>`) + 0.79% (`::<8>`) |
| `itxfm_add_dispatch::{closure#0}` | 1.92% | **gone** (inlined into `for_rows_mut`) |
| `ReconDst::slice_mut` | 1.27% | below the top-25 cut |
| `__arcane_inv_txfm_hbd_neon` | — | 1.28% |
| `_platform_memset` | 1.01% | 0.40% |
| **16bpc itx family, summed** | **10.90%** | **5.07%** |

---

## 4. Correctness

Every gate below was run on this branch at `7bc62f3` unless noted. The corpus
and `wide_exclusion` runs are `nice -n 19` per the brief's E-core rule.

### 4.1 Corpus, by name, with the MD5 in the value

`examples/md5_inventory` against the committed baseline
`benchmarks/aarch64_md5_fixes_2026-08-07_final.tsv.zst`, set-diffed by
`(group, name)` with `(status, actual_md5)` as the value — a change that repairs
five vectors and breaks five is invisible in a count and is N differing rows
here.

| run | entries | PASS | only-baseline | only-head | **differing** | |
|---|---|---|---|---|---|---|
| `--threads 1`, full corpus | 768 | **766** | 0 | 0 | **0** | SETDIFF CLEAN |
| `--threads 8`, minus film grain | 755 | **753** | 0 | 0 | **0** | SETDIFF CLEAN |

The two non-PASS baseline entries are `8-bit/features/annexb` and
`8-bit/features/section5`, both SKIP in the baseline and SKIP here. The skipped
groups at `--threads 8` are **`8-bit/film_grain` (7 vectors) and
`10-bit/film_grain` (6)**, per #479 — film grain takes a whole-plane mutable
guard per row worker and aborts every `--threads > 1` decode with
`apply_grain`; pre-existing on `main`, untouched by this branch.

### 4.2 The per-transform oracle, with its teeth proved

`--features __simd_test` runs the **generic scalar reference** — the conformance
oracle itself, not a transcription — against every SIMD transform call and
panics on any divergence.

| vector | `ITX_MISMATCH` |
|---|---|
| `L{64x36,512x288,1024x576,2048x1152,3840x2160}_420_10b` | 0 |
| `L{256x144,3840x2160}_444_10b` | 0 |
| `v4k_8tile_10b`, `v1024_10b`, `v4k_1tile_10b` | 0 |

**Teeth:** planting `+ 1` on `txfm_core`'s rounding constant makes the FIRST
vector fail — `ITX_MISMATCH size=16x16 type=0 eob=202 nbad=14 max_diff=1`,
rc=101. Restored byte-exact (sha256 + `git diff --exit-code`).

Frame-MD5 identity was also checked directly, base vs each arm, on 12 vectors
spanning both depths, 4:2:0 and 4:4:4, and single- and 8-tile.

### 4.3 `forbid(unsafe_code)`, proved actively in a file that is always compiled

| plant | result |
|---|---|
| `unsafe {}` in **`lib.rs`** (crate root, `forbid` at line 13, compiled on every target) | **`error: usage of an 'unsafe' block`**, build fails |
| `unsafe {}` in `src/safe_simd/itx_arm_hbd.rs` inside its `cfg(target_arch = "aarch64")` region (module `forbid` at line 45) | **`error: usage of an 'unsafe' block`**, build fails |

The first is the non-vacuous one: `lib.rs` has no `#[cfg]` above it. The second
proves the changed module's own attribute is live on this target. Both restored,
sha256 verified, `git diff --exit-code` clean.

### 4.4 Standing hazards, replanted

`crates/rav1d-disjoint-mut` is **byte-identical to `main`** on this branch
(`git diff 2fae4fe -- crates/` is empty), so the tracker cannot regress from a
change that is not there. Replanted anyway, `--features __probe_wide`:

| plant | `wide_exclusion` |
|---|---|
| baseline | ok (0.05 s) |
| in-lock `state` re-read deleted from `add_contended` | **FAILED** (0.07 s) |
| `active()` cut to one shard | **FAILED** (0.00 s) |
| after both restores | **ok** |

Restored byte-exact: `sha256 d4e03d4a…423176d` (the same hash
`MUT_RECON_KERNELS.md` §7 records) and `git diff --exit-code -- crates/` clean.
`touch`ed after each restore, per that section's mtime gotcha.

### 4.5 Threading

| gate | result |
|---|---|
| `cargo test --release --workspace --no-fail-fast` | **240 passed, 0 failed**, 30 ignored, rc=0 |
| `mt_stress` at threads 1/2/4/8/16, 5 trials each | **25/25 ok** |
| `multi_decoder_pressure.sh` — 12 concurrent decoders x 3 iters, thread counts 1/2/4/8/16, 5 vectors | **PASS**, every md5 matches the serial reference |

### 4.6 Miri, both models

`cargo +nightly miri test -p rav1d-disjoint-mut --no-fail-fast`.

| model | result |
|---|---|
| Stacked Borrows (`MIRIFLAGS=""`) | **rc=0**, no UB; `soundness` 5 passed in 992.7 s, plus 25 + 1 + 1 across the other targets |
| Tree Borrows (`-Zmiri-tree-borrows`) | **rc=0**, no UB; `soundness` 5 passed in 993.1 s, plus 25 + 1 + 1 |

### 4.7 Compile matrix and lints

| configuration | result |
|---|---|
| `cargo clippy --no-default-features --features "bitdepth_8,bitdepth_16" -- -D warnings` (the CI leg), aarch64 | **clean** |
| the same on `--target x86_64-apple-darwin` | **clean** |
| `cargo check --all-targets`, default features | no new warnings — the 90 present are pre-existing dead-code in test targets, none in `ablate.rs`, `itx_arm_hbd.rs`, `itx_arm.rs` or `itx_shape_census.rs` |
| **DEBUG** profile, `cargo test --lib itx_arm_parity` (debug asserts + overflow checks live) | **8 passed**, including `itx_10bpc_matches_scalar_{small,large}_coeffs` and `itx_10bpc_every_eob_small_coeffs` |
| `cargo fmt --all -- --check` | clean |
| `cargo clippy --features "c-ffi,bitdepth_8,bitdepth_16"` | **pre-existing failure on macOS**, unrelated: c-ffi's `EAGAIN` const-assert is 35 on macOS vs 11 on Linux. Documented in `MUT_RECON_KERNELS.md` §7b; `main` fails it identically. |

---

## 5. Method, and what weakens these numbers

- Every timed run went through `measlock`, which now has a **`--load-ok`** mode
  (added this session, installed by atomic rename so a running `measlock` cannot
  be corrupted mid-flight — see `SIZE_SWEEP.md`'s trap 1). It keeps the mutual
  exclusion and skips the politeness wait. The previous behaviour on a box with
  a long-running non-timed job was the worst of both: wait 20 minutes, then run
  anyway.
- **The box was never idle.** Another agent held a multi-hour `miri` job at
  ~100% of one core for this whole session, and my own gate runs (`nice -n 19`,
  per the brief's E-core rule) add to it. Every row carries `foreign_max`.
  **Absolute ms/frame here is inflated and is not comparable to an idle
  campaign's; the arm-vs-arm and ours-vs-dav1d ratios are paired inside a round
  with a rotating arm order, and those are the statistic to read.**
- Two-point wall fit (`total = a + b*frames`) at per-cell frame counts, the same
  instrument as `scripts/perf/verify_gap.sh` and `SIZE_SWEEP.md`.
- No `nice` on any timed run. No `-C target-cpu=native`. dav1d 1.5.4
  `--framedelay 1` interleaved in the same sweep.
- **8bpc is a layout control, not a result.** All three changes are inside
  `BD::BPC == BPC16`, so any 8bpc movement is instruction-cache and code-layout
  noise. An early n=3 probe saw the 8bpc arm swing 198.5 → 195.1 → 199.3 ms
  across base/A/AB with disjoint bands — **±2% of pure layout noise on a depth
  the diff cannot reach**. Treat sub-2% 8bpc deltas anywhere in this repo with
  that in mind.

---

## 6. What the next round should take, by measured cost

1. **`decode_coefs`, 54.84% → 59.25% of self time.** It is the entropy decoder
   and it is more than half the frame at both depths and every size, and
   `SIZE_SWEEP.md` measured its per-pixel cost as *flat* across 3.5 decades. It
   is not the hump and it is not the depth penalty; it is simply the largest
   thing, by a factor of nine over the next symbol. Nothing in fifteen rounds
   has attacked it.
2. **The borrow tracker's context-array traffic.** 10.13% inclusive at
   1024x576 8bpc — the hump cell — and its callers are `decode_b`,
   `ctx::CaseSetter::set_disjoint`, `rav1d_recon_b_intra`, `decode_sb`. Note
   #455's standing warning: reducing registration COUNT has measured null twice;
   what paid was shard GRANULARITY. `set_disjoint` writes small fixed-size
   context arrays, which is a different shape from both.
3. **The remaining 5.07% of 16bpc itx** — `Fn::call` 1.34%, the driver 1.28%,
   `apply1d::<16>` 1.16%, `apply1d::<8>` 0.79%. Halved already; the rest is real
   transform work at 4 lanes against 8bpc's 8, which is also what dav1d does.
4. **`cdef_filter_block_16bpc_inner`'s memmoves** — 165 of the frame's 466
   `_platform_memmove` samples, and **zero** from the 8bpc twin. 16bpc CDEF is
   otherwise *faster* than 8bpc CDEF, so this is a padding/copy artefact, not
   the filter. Suspect `let mut tmp = [CDEF_VERY_LARGE; TMP_LEN]`: a `u16`
   splat of `0x8000` cannot become a byte `memset` the way the 8bpc twin's can.
5. **The 8bpc dispatch has a small-rectangle hole that the 16bpc one does not.**
   `itxfm_add_dispatch`'s 8bpc arms cover 4x4, 8x8, 16x16, 32x32, 8x32, 32x8,
   16x32, 32x16 and the 64-family — but **not 4x8, 8x4, 4x16, 16x4, 8x16,
   16x8**, which fall through to the scalar reference. The hbd path covers all
   nine small shapes including those rectangles, so 16bpc has *better* small-rect
   coverage than 8bpc. Measured share, from the census: **593 of 23,444 calls
   (2.53%) and 3.28% of coefficient area at 1024x576 8bpc** — the hump cell —
   against 8 calls (0.01%) at 4K 8bpc, because 4x8/8x4 are barely coded there.
   Small on these vectors, a real hole on rect-heavy content. Not built: the
   payoff on anything measured here is a fraction of `Fn::call`'s 1.63%, and the
   kernels need per-shape bit-exactness proof before they can be wired.
